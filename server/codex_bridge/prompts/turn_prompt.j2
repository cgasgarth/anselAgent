Plan the next Ansel response for this request.

Goal: {{ goal_text }}
Latest user message: {{ latest_user_message }}
Refinement: mode={{ refinement_mode }}, pass={{ pass_index }}/{{ max_passes }}
Tool budget: maximum {{ max_tool_calls }} tool calls in this run.
Image: {{ image_name }} ({{ width }}x{{ height }})
{% if exif_line %}EXIF: {{ exif_line }}
{% endif %}
Available playbooks:
{% for playbook in playbooks %}- {{ playbook.id }} ({{ playbook.category }}): {{ playbook.summary }} Use when: {{ playbook.selectionHint }}
{% endfor %}
{{ module_policy_summary }}
{% if module_cards_summary %}

{{ module_cards_summary }}
{% endif %}

Tool usage:
- This turn is an image-editing workflow. Use only these tools: `get_playbook`, `get_image_state`, `get_preview_image`, and `apply_operations`.
- Do not use command execution, file editing, patching, filesystem, or any other generic workspace tool.
{% if has_edit_graph -%}
- Turn input already includes an authoritative `editGraph`, legacy editable settings, histogram, compact analysis signals, and (in live mode) the preview image.
{% else -%}
- Turn input includes legacy editable settings, histogram, compact analysis signals, and (in live mode) the preview image. No authoritative `editGraph` is available for this turn.
{% endif %}
- If the user's prompt is underspecified, infer likely photo type and style direction from the provided preview image and analysis signals.
- Use read-only tools (get_image_state, get_preview_image) only when you need refreshed state after edits.
- Use get_playbook only when it is likely to materially change the edit plan or reduce real uncertainty. Do not browse playbooks speculatively.
- If the scene is clear, start from the strongest matching photo-type playbook; do not fetch conflicting or weakly related photo-type playbooks.
- Treat style playbooks as optional refinements on top of the scene understanding, not substitutes for photo-type selection.
- For style playbooks, require direct evidence from the request, preview, EXIF, or image-state signals, and fetch them only when they would materially change editing priorities.
- get_playbook is preparatory only: once you have enough context to pick a direction, the next substantive step should usually be apply_operations rather than more research.
- Stay inside the Ansel tool loop for the entire turn: infer scene, optionally fetch one high-value playbook, call apply_operations, inspect refreshed state only if needed, then finalize the JSON response.
- apply_operations returns the refreshed preview automatically; use get_preview_image only for extra visual checks.
- In live mode, apply_operations also returns a verifier summary JSON block; if verifier status is fail, keep refining before finalizing.
- In live mode, every actual edit must be performed through apply_operations. Do not introduce new operations in the final JSON that were not already applied successfully.
- Before finalizing, consider whether additional provided controls would materially improve tone, color, detail, or noise; do not stop at basic exposure/contrast edits when stronger supported tools are available.
- Always optimize toward refinement.goalText.
Live run mode is enabled: use apply_operations for iterative edits inside this same run.
{% if has_edit_graph -%}
- Prefer graph-based operations against `editGraph` nodes and properties. Use `target.type = "graph-control"`, `graphId = "edit-pipeline"`, and the exact provided `nodeId` + `propertyPath`.
- Graph operations should normally use `kind = "set-graph-prop"` with the same value shape you would use for the resolved control (`number`, `choiceValue`, or `boolValue`).
{% else -%}
- No `editGraph` is available in this run. Do not invent graph controls, `nodeId`s, or `propertyPath`s.
- When `editGraph` is absent, use legacy editable settings and canonicalActions only.
{% endif %}
For this multi-turn path, you may pass `canonicalActions` to apply_operations instead of raw operations for these supported intent-level edits: `adjust-exposure`, `adjust-white-balance`, `recover-highlights`, `reduce-noise`, `grade-color`, `rotate`, `crop-normalized`, `crop-to-bounding-box`.
Canonical fields: `adjust-exposure` uses `exposureEv`; `adjust-white-balance` uses `temperatureDelta`, `tintDelta`, and/or `presetChoiceId`; `recover-highlights` and `reduce-noise` use `strength`; `grade-color` uses `target` + `amount`; `rotate` uses signed `angleDegrees` for precise rotation deltas, with positive values rotating right and negative values rotating left.
For crop/framing, use canonicalActions only: `crop-normalized` uses normalized `left`, `top`, `right`, `bottom`; `crop-to-bounding-box` uses normalized `boxLeft`, `boxTop`, `boxWidth`, `boxHeight`, plus optional `paddingRatio`, `aspectRatioWidth`, and `aspectRatioHeight`.
When the user specifies an aspect ratio such as 4:5, 3:2, 16:9, square, portrait, or widescreen framing, include `aspectRatioWidth` and `aspectRatioHeight` so the final crop matches that ratio exactly.
Never invent raw crop/framing graph refs such as `group:clipping:*`, `group:crop:*`, or rect-edge properties. Those controls are intentionally hidden and will be rejected.
The runtime binds supported canonical actions to concrete Ansel controls deterministically before applying them.
Inside each apply_operations call, operations are auto-applied one at a time with a fresh render after each step.
{% if has_edit_graph -%}
Turn input includes the current preview image, the authoritative editGraph, legacy editable settings, and histogram.
{% else -%}
Turn input includes the current preview image, legacy editable settings, and histogram; no authoritative editGraph is available.
{% endif %}
After each apply_operations call, inspect the refreshed preview and re-check get_image_state when you need refreshed exact state.
Apply at least one edit batch within the first {{ apply_budget_window }} tool calls.
After calling get_playbook, the next non-final action should usually be apply_operations unless refreshed image state is strictly required.
When satisfied, return final JSON with continueRefining=false and usually empty operations.
In multi-turn mode the final JSON should summarize the run; continueRefining must be false.
Respect refinement state: treat passIndex/maxPasses as budget, set continueRefining=false once safe gains are exhausted.
Return only the JSON object required by the output schema.
